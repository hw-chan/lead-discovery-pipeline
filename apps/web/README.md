# Web App

React/Vite client for the lead discovery pipeline.

Use the root README for setup and run instructions. The web app expects the API to be running and uses `/api` routes through the Vite proxy during local development.
